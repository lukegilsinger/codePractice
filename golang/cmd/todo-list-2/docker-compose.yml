# docker-compose.yml
version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - DATABASE_URL=data/todos.db
      - LOG_LEVEL=info
      - JWT_SECRET=local-dev-secret
    volumes:
      - ./data:/app/data